import 'package:flutter/material.dart';

class VehiclesScreen extends StatelessWidget {
  const VehiclesScreen();

  @override
  Widget build(BuildContext context) {
    return const Text('Vehicle Screen is here');
  }
}
