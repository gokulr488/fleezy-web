import 'package:flutter/material.dart';

class DriverManagementScreen extends StatelessWidget {
  const DriverManagementScreen();

  @override
  Widget build(BuildContext context) {
    return const Text('Driver management Screen');
  }
}
